import countdown, { formatMap } from '..';

/**
 * 
 * @func   倒计时 (从现在开始到指定时间)
 * @param  $endAt     unix_time  结束时间
 * @param  formatter  string
 * @return            string  
 */
export const countdownFromDuration = ($duration: number, formatter?: string[]) => {

};
export default countdownFromDuration;