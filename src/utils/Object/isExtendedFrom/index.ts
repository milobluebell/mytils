/**
 *
 * isExtendedFrom($subObj, $superClass) - 🍀 判断对象/类是否继承自某一对象/类
 *
 * @param   $key         待推断属性的key
 * @param   $targetObj   被推断对象
 */
const isExtendedFrom = ($subObj: any, $superClass: any): boolean => {
  return false;
};
export default isExtendedFrom;
